# Smart Campus Management System - Frontend

This frontend is now wired for:
- Authentication (register / login / logout)
- Student attendance marking
- Faculty attendance session creation
- Assignment creation
- Assignment submission with file upload
- Faculty submission viewer

## Run

```bash
npm install
npm run dev
```

Create a `.env` file from `.env.example` if your backend runs on a different URL.
